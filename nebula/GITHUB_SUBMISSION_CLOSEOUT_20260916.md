# GitHub submission publication

The owner explicitly requested publication to `https://github.com/jaikaushik-prog/nebula-ctle-rl` after being unable to submit the2.27GB convenience ZIP. This update publishes the current code and selected saved evidence, not that ZIP. Report and video remain separate owner submissions.

## Changes

- Main README is now a Nebula-first landing page with startup commands, deliverables, the selected352 circuit, automatic288-to352 recovery, measured results, source map and concise evidence boundaries.
- `docs/SUBMISSION_GUIDE.md` gives a saved-result tour, direct evidence links, restoration instructions and separate fresh-SPICE requirements.
- Current web, recovery, language-wrapper, generated-receiver and programmable-receiver code is included with its existing tests and saved evidence.
- Six large new waveform/terminal files are distributed as verified gzip siblings. `nebula.restore_submission_evidence` restores exactly the original bytes using `submission_compressed_evidence.json`. It never runs simulations or silently overwrites changed local evidence.
- Existing reports, video/narration, private keys, PDK installations, external reference material and the convenience ZIP are not added by this update. Existing Git history is retained; no force-push or history rewrite.

## Validation boundary

Before publication changes,34 focused saved-app tests passed. Nine new restoration tests passed; the final focused suite passed92tests in11.79s. The largest newly staged file is17.273089MB. Exact staged scientific bytes and real missing-file restoration are checked before commit. The earlier isolated-package check loaded both saved workflows, all selected/programmable downloads, the independent reference, saved diagnostics, physical registry pins and five frozen policy sets, with zero simulator calls. This publication additionally checks restored bytes and staged scientific evidence against original hashes.

No fresh SPICE, training, complete design job or full regression is authorized by publication. These are software/distribution checks, not new circuit verification. Original experiment manifests, measurements and scientific failures remain unchanged. Current report/video files are not rewritten.

Publication occurs on16September2026 and does not imply that the code was uploaded before the competition deadline. Completion is established by the remote branch commit matching the local publication commit, reported to the owner after push.
