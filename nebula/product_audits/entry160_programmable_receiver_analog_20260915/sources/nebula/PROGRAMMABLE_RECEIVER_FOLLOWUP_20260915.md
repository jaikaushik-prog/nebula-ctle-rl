# Entry160 retained initialization failure and separate followup

Registered 15 September 2026 after the first two charged calls, before followup measurements. The zero-call preflight and both held-state0 batches remain immutable. The first used measured state0 seeds; the second used the earlier state1 resolved seeds. Both solve q approximately equal to qb at the baseline and fail the required negative-state gate. Finite AC curves at those unresolved points are diagnostic only, not target matches. The first batch's generic NO_MATCH_IN_BOUNDED_SAMPLE status does not establish an electrical target failure: its instrument_ok is false. The second correctly says INCOMPLETE_OR_INVALID.

This followup makes separate measurements; it does not retry or retrospectively pass either failed batch. The DUT, voltage bounds, stimuli and thresholds remain unchanged. Existing registered controls (.73,.195), (.73,.210), (.73,.225) are eligible nominal link cases, in that order. These are exploratory preselected controls, not controls certified by the failed AC instrument. A clocked waveform can demonstrate finite-pattern function even when a held-DC analysis does not initialize; label those scopes separately.

First use one independent clocked nominal link at .73/.195 and one held-state1 OP/AC/noise measurement at .73/.195 without repeated alter operations. If the held-state1 instrument is valid and negatively resolved, use its actual measured q/qb/mp/mn values as released NODESET guesses for a separate state0 measurement on the same selected-derived DUT. Do not force state with .ic/UIC or ideal decision voltages. If it is unresolved, retain that failure and do not launch a full PVT campaign.

If both held states resolve, a new small batch over the three listed controls may use the new resolved seed and the original repeatability, voltage, shape and 0.5dB/100MHz matching gates. A matching measured control may then receive its own clocked link check. Do not claim automatic calibration on the basis of an invalid curve. Full signed model-domain failures remain disqualifying for receiver signoff, even if signal and target checks pass. Whole task still stops by14:34:35UTC, scientific launches by14:04:35UTC. All followup calls are charged together with the earlier two calls in final reporting.
