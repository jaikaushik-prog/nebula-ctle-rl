# Entry160 nominal analog completion at the measured control

Registered after seven charged calls. The new receiver now matches6dB/2.1GHz within the existing internal0.5dB/100MHz criterion in both held clock states at R-control .73VDD and C-control .21VDD. It passes the nominal finite-pattern transistor signal gate. Signed model-domain checks remain false. Do not promote it to a signed-off receiver or transfer PVT.

Use the same new DUT and chosen controls, with all internal NODESET guesses from that same DUT's valid measured held-state row. Two independent held-state OP/AC/noise calls recheck response consistency and integrate noise over10MHz-5GHz with independent spectrum reintegration. The state1 measured internal values seed clocked100MHz differential100mV-peak tone checks at maximum timesteps5ps and2.5ps, reltol1e-5, vntol1e-7, abstol1e-12, gear/maxord2 (the existing accepted instrument settings). Capture150ns; apply the unchanged50-150ns harmonic window, split-window stability and cross-resolution comparisons. Inspect actual initial negative memory state and every drawn terminal.

Stop the analog schedule on an invalid instrument, missing endpoint, unknown warning or timeout. At most four new calls, each <=180s, all separately charged in the existing2.5-hour authorization. The more restrictive14:04:35UTC science cutoff remains. No PVT, new device geometry, reward changes or threshold changes. Held-state noise is not periodic receiver noise; clocked distortion is at the loaded CTLE output, not a BER or full receiver signoff claim. The known signed failures remain visible regardless of passing analog scalar values.
