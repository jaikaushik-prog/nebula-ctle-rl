# G4_RESULTS.md — a corner-robust design, generated and verified

**Session 22k-run, 2026-08-20.** `python -m nebula.experiments.exp_g4_verify
--run` — **675 simulations, 3.7 minutes**. Artifact
`nebula/experiments/g4_verify_results.json`. The prediction was committed in
the module's docstring at `8f73677`, before the run.

---

## 0. The verdict

**G4's criterion — *"corner-robust design generated and verified; results table
drafted"* — is MET, 23 days early.** And the verification caught something the
search could not have.

| | |
|---|---|
| designs the framework certified as corner-robust | **2** (P3 rung, `uniform`, 2 of 20 seeds) |
| verified at **45 corners × 3 loads = 135 points** | **1 of 2 passes all 135** |
| the other | **fails 8 of 135 — and all 8 are at corners the screen never evaluates** |
| controls (strongest TT-only designs, 8.9995 / 8.9993 / 8.9993) | **75, 22 and 9 of 135 fail** — the gate can fail |

> **The 3-corner screen missed every failure. Every one of the 8 failing points
> is at a MIXED process corner — `sf` or `fs` — and the screen has no member of
> either.**

---

## 1. What was generated, and by what

**Not by hand, and not by RL.** The benchmark's P3 rung scores every proposal on
the worst of 3 screen corners × 2 loads. Its `uniform` arm found two designs
that cleared every spec at all six points, in 20 seeds — rewards **8.0342** and
**8.0021**, against a feasibility bonus of exactly 8.0. Both are marginal by
construction: they are the designs that only just qualify.

Their sizing vectors are read out of the run log by
`exp_g4_verify.candidates()`, so no number in this document was transcribed by
a human.

**Stated plainly for the report: the corner-robust design was found by uniform
random search, not by the RL policy.** G4's criterion says "generated", not
"generated by RL". `rl/corner_env.py` (session 22k) now makes worst-corner
scoring available *inside* the RL loop, and has not yet been run at scale.

---

## 2. The verified design

`57cba07581cd2603`, from `uniform` replicate 4. **135 of 135 points pass.**

### 2.1 The schematic — the competition's literal deliverable

| parameter | value |
|---|---|
| `w_in` | 84.41 µm |
| `l_in` | 222.9 nm |
| `nf_in` | 4 |
| `i_bias` | 1.1376 mA |
| `rs` (full source-to-source) | 430.9 Ω |
| `cs` | 3.217 pF |
| `rl` | 240.5 Ω |
| `vcm_in` | 1.5889 V |
| `cl` (context) | 32.63 fF |

Drawn SKY130 devices throughout — real resistors, a real MIM capacitor, a real
current mirror. No ideal elements except `I_ref` (`TAIL_DEVICE.md` §0).

### 2.2 The resulting specs, at TT/1.00/27 °C, `cl_mid`

| spec | requirement | measured | |
|---|---|---|---|
| S3 peaking | 3–12 dB | **9.780 dB** | pass |
| S3 peak frequency | 1.25–2.5 GHz | **1.8906 GHz** | pass |
| S3 boost at Nyquist | > 0 (our reading) | **+9.736 dB** | pass |
| S5 input noise | < 1.5 mV_rms | **0.2117 mV_rms** | pass, 7.1× margin |
| S6 power | < 15 mW | **2.1616 mW** | pass, 6.9× margin |
| pair saturation | > 0 | 0.8431 V | pass |
| tail saturation | > 0 | 0.5760 V | pass |
| DC gain | — | −3.443 dB | (attenuates at DC; boosts at Nyquist) |

The reading `CLAUDEwa.md` §3 insists on is satisfied both ways: the peak-to-DC
ratio is inside the band **and** the response is 9.7 dB **up** at Nyquist, so
this is an equaliser rather than a stage that peaks below the window.

### 2.3 Its worst case over the full grid

| | |
|---|---|
| worst reward over 135 points | **8.0210** |
| worst point | **`fs/0.95/125 °C`, cl = 78.0 fF** |
| binding spec there | `S3_f_peak` |
| **is that corner screened?** | **No** |

The ten tightest of its 135 points span **four different process corners** —
`fs` ×3, `ss` ×3, `tt` ×3, `sf` ×1. The worst case genuinely moves around the
box; there is no single corner that stands in for the rest.

---

## 3. The design that failed, and why it matters more

`c9d52866743dc1`, from `uniform` replicate 16, claimed **8.0021** at the screen.
**8 of 135 points fail.**

| corner | load | reward | verdict | binding |
|---|---|---|---|---|
| `ss/1.05/125 °C` | 78.0 fF | **−10.0000** | **invalid** | — |
| `sf/1.05/125 °C` | 78.0 fF | **−10.0000** | **invalid** | — |
| `sf/0.95/0 °C` | 13.6 fF | −0.0121 | valid | `S3_f_peak` |
| `sf/1.00/0 °C` | 13.6 fF | −0.0171 | valid | `S3_f_peak` |
| `sf/1.05/0 °C` | 13.6 fF | −0.0237 | valid | `S3_f_peak` |
| `fs/0.95/125 °C` | 78.0 fF | −0.0104 | valid | `S3_f_peak` |
| `fs/1.00/125 °C` | 78.0 fF | −0.0203 | valid | `S3_f_peak` |
| `fs/1.05/125 °C` | 78.0 fF | −0.0210 | valid | `S3_f_peak` |

**None of the eight is a screened corner.** The screen is
`ss/0.95/125 °C`, `ff/1.05/0 °C`, `ss/0.95/0 °C` — all-slow and all-fast. Every
failure is at a **mixed** corner, `sf` or `fs`, of which the screen has no
member, plus one all-slow point at the *high* supply the screen does not carry.

The mechanism is legible: the mixed corners skew the NMOS/PMOS balance, which
moves the bias point and therefore `gm`, and `f_peak` follows. Neither all-slow
nor all-fast reproduces that skew, so the screen is blind to it by construction
rather than by bad luck. Six of the eight failures are `S3_f_peak` misses of
**0.01–0.02 octaves** — the design is a hair outside the window, at corners
nothing looked at.

---

## 4. The control, which is what makes this a gate

A verification that only runs on designs expected to pass cannot fail (G73). So
the same 135 points ran on the **three strongest TT-only designs** the
benchmark produced — 8.9995, 8.9993, 8.9993, near the reward ceiling, feasible
at nominal and never certified at any corner.

| design | claimed at TT | corner points failed |
|---|---|---|
| `6484611d7a84` | 8.9995 | **75 of 135** |
| `6bf71903821b` | 8.9993 | **22 of 135** |
| `f4829115423c` | 8.9993 | **9 of 135** |

All three fail. **The gate can fail, and it was demonstrated rather than
asserted.** And the comparison is the argument for contribution #1 in one line:
*the best design at nominal fails 75 of 135 corner points; a design scored on
its worst corner from the start passes all of them.*

---

## 5. The prediction, scored

Committed in the module docstring at `8f73677`, before the run.

| # | prediction | measured | |
|---|---|---|---|
| 1 | both designs pass all 135 points; **band: at least 1 of 2** | **1 of 2** | band **held**, point **MISS** |
| 2 | the binding corner is one of the three screened ones | **it is not** — `fs` for the passer, and all 8 failures unscreened | **MISS**, decisively |
| 3 | the binding spec is `S3_f_peak` at both | true for the passer and for 6 of 8 failures; the other 2 are invalid | HIT |
| 4 | the two bind at **opposite** ends (slow-hot-heavy vs fast-cold-light) | both are hurt at hot-heavy *and* cold-light, through the MIXED corners | **MISS** |

**Three misses, and they are the result.** I predicted the 3-corner screen
would be adequate because G47 measured it at 98.7 % of what 45 corners catch.
It was not. See §6.

---

## 6. G99 — a screen calibrated on the POPULATION is not a screen for its own SURVIVORS

G47 measured that the three screen corners capture **98.7 %** of what all 45
catch. That number is a statistic over **the whole box**, where most designs
fail obviously and any corner catches them.

**Survivors are the opposite population.** They are, by construction, the
designs sitting on the boundary — both of ours scored within 0.035 of the
feasibility bonus. On the boundary, a 1.3 % blind spot is not rare: **1 of our
2 survivors failed, and 100 % of its failures were in the blind spot.**

> **A screen's accuracy on the population it was calibrated on says nothing
> about its accuracy on the designs it lets through.** Measure a screen on its
> own output, not on the box.

Recorded as **G99** in `HANDOFF.md` §9. The practical consequence: the screen
stays as a *search* device — it is 7.5× cheaper than the full grid and it did
put both survivors within reach — but **no design may be called corner-robust
on the screen alone.** Verification is the full grid, and it costs 135
simulations, which is under 15 seconds.

---

## 6b. The full slide — all eleven spec rows, at every corner

**Added session 22o.** The competition's own specification table lists eleven
rows. The scored objective had **seven**: S4 (HD3) and S7 (area) had no
tolerance row at all, and the two S8 (eye) rows existed but were unreachable
through `reward()`. A judge holding that slide would have found three blanks.

`exp_g4_verify --full` scores every row at 45 corners × 3 loads, using the G2
closed-loop chain rather than the search evaluator, because three of the rows
are not in the search's measurement vector:

    run_point(swing=True, ac_sweep=True, hd3=True)   <- S4 needs the transient
      -> device_result_from_point()                   <- carries HD3 and area
        -> evaluate_link()                            <- S8, no extra SPICE call

### The verified design, `57cba07581cd2603`

| spec row | verdict | checked at | failed at |
|---|---|---|---|
| S3 peaking | **PASS** | 135 | 0 |
| S3 peak frequency | **PASS** | 135 | 0 |
| S3 Nyquist boost | **PASS** | 135 | 0 |
| **S4 HD3** | **PASS** | 135 | 0 |
| S5 input noise | **PASS** | 135 | 0 |
| S6 power | **PASS** | 135 | 0 |
| **S7 area** | **PASS** | 135 | 0 |
| S8 eye height | **NOT MEASURABLE** | 0 | — |
| S8 eye width | **NOT MEASURABLE** | 0 | — |
| pair saturation | **PASS** | 135 | 0 |
| tail saturation | **PASS** | 135 | 0 |

**Nine of eleven rows pass at every one of 135 points. Two are not measurable,
for a named and measured reason.**

### S4 and S7, the two rows that had no home

Both pass by a wide margin at every corner, which is the finding rather than a
reason to have left them unscored:

| | measured | limit | margin |
|---|---|---|---|
| **HD3** | −47.7 to −49.2 dBc | < −30 dBc | **17.7–19.2 dB** |
| **area** | 0.002150 mm² | < 0.05 mm² | **23× inside** |

`CLAUDEwa.md` §3 called S4 *"relatively relaxed — do not over-engineer"*. That
is now measured across the full PVT grid rather than asserted.

### S8 is blocked by COMPRESSION, and that is the honest answer

The link layer refuses to compute an eye, at all 135 points, with:

    output swing 335–941 mVpp exceeds the linear limit ~147–339 mVpp

**This is not a defect in the verification — it is the verification working.**
The eye is built from a small-signal pole-zero fit, and at the PCIe input drive
level this CTLE is driven past its own measured linear limit, so the model the
eye rests on no longer applies. `evaluate_link` returns `ok=False` rather than
a plausible number, which is exactly what `CLAUDEwa.md` §8 rule 1 demands.

It is also **not new**: `G2_RESULTS.md` measured it on 168 of 276 designs
(61 %) with a median overshoot of 1.29×, and `HANDOFF.md` §8 lists it as *"the
top open design question and it is a human's call"*, with three routes on
record — reach below S3's 3 dB floor, declare the low-loss end of the channel
family out of scope, or accept that the CTLE must attenuate and re-derive the
box's `rl` range downward.

**What session 22o adds is that the blockage now holds for the DELIVERED
design at every corner, and is reported per row rather than silently absent.**
The median output swing of the verified design is **335.2 mVpp**.

### The control still fails, and still on the same row

| design | rows passing | rows failing | not measurable |
|---|---|---|---|
| `57cba07581cd` (corner-robust) | **9** | 0 | 2 |
| `c9d52866743d` (screen survivor) | 8 | **1** — `S3_f_peak` at 6 points | 2 |
| `6484611d7a84` (best at TT only) | 8 | **1** — `S3_f_peak` at **66** points | 2 |

The nominal-only control fails the peak-frequency row at **66 of 135 points**.
S4, S7 and every other measurable row pass for all three — so the corner axis
is entirely a story about `S3_f_peak`, which is what §3 already showed.

---

## 7. What is still open

* **The corner-aware RL loop has not been run at scale.** `rl/corner_env.py`
  exists and is tested; the design verified here came from uniform random
  search. Running PPO on the corner-aware objective is the next experiment and
  needs `baselines.method_ppo`'s structural refusal lifted.
* **Should the screen gain a mixed corner?** `sf` and `fs` are where both
  survivors were tightest. Adding one would raise the search cost from 3
  corners to 4 (+33 %) and is a change to the benchmark, so it is an owner's
  decision (`CONTINUE_HERE.md` §5).
* **Two failures were `invalid`, not merely out of spec** — non-convergence or
  an unusable measurement at `ss/1.05/125 °C` and `sf/1.05/125 °C`, both at the
  heavy load. Whether that is a real circuit failure or a simulator failure is
  unmeasured, and G26/G30 say an exit code will not tell you.
* **S8 (the eye) is not in this verification.** `reward_v1` scores `V1_SPECS`,
  which excludes S8; the bridge exists (`G2_RESULTS.md`) but wiring it into the
  corner sweep is a separate re-run event.
