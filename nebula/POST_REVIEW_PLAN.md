# Entry 113 - post-review attribution and physical coverage
Date: 2026-09-07. Owner authorised the review follow-up plan.
This is a supplemental, post-review diagnostic: Entry 89 FINAL is already exposed.
No new held-out claim, training, reward change, range change or policy selection.
Keep every historical experiment and product artifact unchanged.

## Frozen protocol (before new results)
A. Replay all 2,430 Entry 89 FINAL identities from the hash-checked midpoint
journal. Map starts from the frozen DEVELOPMENT lookup exactly as Entry 89.
Use five frozen PPO checkpoints and their five matching pre-PPO imitation
checkpoints; verify manifest hashes before deserialisation. No training.
Compare fixed start, uniform legal local moves (20 persistent seeded streams),
the existing visible-eye hill climb, imitation, and PPO. Also report uniform
global sampling without replacement as a practical sizing baseline with a
different move space. All use the identical best-compliant-visited shield.
No proposer sees compliance, q, hidden identity or unvisited measurements.
Generate original eight-visit trajectories, honour policy LOCK, and score
prefixes capped at 2, 4 and 8 visits. Keep the trained observation's eight-visit
normalisation unchanged; prefixes are external interruption, not retrained
short-horizon policies. Bill every repeated visit. Report actual visits,
unique visits, compliance, quality, proposal time and evaluation time.
A fixed start uses one visit; unused budget is not billed. No padded visits.
Verify eight-visit PPO results reproduce the historical per-identity result.
Report per-seed metrics and paired quality/compliance differences. Bootstrap
54 request/loss blocks (all 45 PVT rows and seed-averaged within each block),
10,000 draws, seed 20260907113. This interval is conditional on this circuit
library, not confidence across unseen hardware. Incremental PPO evidence
requires positive quality CI versus imitation and classical controls without
lower mean compliance; otherwise report the appropriate null or trade-off.
Predictions: shield retains all compliant starts; PPO replay matches; the
winner versus imitation/random/hill is unknown. No outcome-dependent reruns.

B. Audit final physical raw warnings and referenced resistor cards read-only.
Record ignored nonlinear terms and model/source hashes. Do not invent a
conversion of unsupported PDK coefficients or silently replace resistors.
If there is no validated equivalent supported implementation, qualify HD3
and leave omitted passive distortion NOT_VERIFIED. No redesign authorised
by a warning disappearing alone.

C. Physical coverage uses exactly the existing 12 requests:
peaking 3,6,9,12 dB crossed with frequency 1.25,1.9,2.5 GHz.
Run the existing classical fixed intersection first; empty means BANK_NO_FIXED
not physical impossibility. For eligible requests use the existing physical
workflow unchanged, at most 137 fresh calls/request, sequentially. Maximum
1,644 calls overall; no candidate retries, training or circuit modification.
Every result keeps project matching tolerances separate from absolute brief
bounds. Preserve all attempted candidates, failures, raw outputs and hashes.
Compare selected setting and selection runtime with RL bypassed using the
same fixed intersection. Singleton intersections establish deterministic
selection independence, not a general RL timing claim.
Prediction: endpoint coverage is incomplete; exact outcome unknown.
Stop after this enumerated matrix; no widening the bank from its results.

## Reporting and completion
Correct shield, HD3, eye, DFE and cost qualifications in the report. Preserve
the previous PDF in the workspace before rebuilding, render and inspect pages.
Record measured offline stage times separately from new request times.
Package a private source/evidence release with hashes; no copyrighted references,
PDK redistribution, credentials or public upload. A live LLM bonus is only
claimed if an actual provider-backed interaction can be demonstrated.
Run required full tests before and after changes. No failing commit.

